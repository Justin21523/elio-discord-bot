// Centralized cooldowns/limits (ms or counts per day)
export const COOLDOWNS = {
  lootPullMs: 5000,
  lootDaily: 50,
  guessMs: 1500,
  diceMs: 1500,
  battleTurnMs: 20000,
  minigameStartMs: 10000,
  inventoryUseMs: 1000,
};

export default COOLDOWNS;
